<% provide(:title, 'New Statement') %>
<h1>New Statement</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@statement) do |f| %>
      <%= f.label :content %>
      <%= f.text_area :content %>

      <%= f.submit "Create Statement", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
